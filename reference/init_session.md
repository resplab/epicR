# Initializes a model. Allocates memory to the C engine.

Initializes a model. Allocates memory to the C engine.

## Usage

``` r
init_session(settings = get_default_settings())
```

## Arguments

- settings:

  customized settings.

## Value

0 if successful.
