# Returns events specific to an agent.

Returns events specific to an agent.

## Usage

``` r
get_agent_events(id)
```

## Arguments

- id:

  Agent number

## Value

dataframe consisting all events specific to agent `id`
